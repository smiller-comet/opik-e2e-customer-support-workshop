# do not add secrets here, use .env file instead
models:
  openai: "gpt-4o"
  anthropic: "claude-sonnet-4-6"
model:
  temperature: 0.7
  max_tokens: 4096
  top_p: 1.0
  stream: false
project:
  name: "Ohm Sweet Ohm Workshop"
  dataset_name: "Customer Support Agent Dataset"
